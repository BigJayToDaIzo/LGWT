package main

import (
	"io"
	"os"
)

func Countdown(w io.Writer) {
	w.Write([]byte("3\n"))
	w.Write([]byte("2"))
}

func main() {
	// stdout for main
	Countdown(os.Stdout)
	// http module next? Countdown FOR TEH WEBZ?
}
